import React from 'react';

const NotFound: React.FC = () => {
    return (
        <React.Fragment>
            <h1>hi from NotFound</h1>
        </React.Fragment>
    );
};

export default NotFound;
